export type City = {
  cityPrefix: string;
}

export type LatitudeLongitude = {
  latitude: number;
  longitude: number;
}

export type Forecast = {
  latitude: number;
  longitude: number;
  units: string;
}